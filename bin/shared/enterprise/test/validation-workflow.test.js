import { ComprehensiveValidationWorkflow } from '../validation-workflow.js';

describe('ComprehensiveValidationWorkflow', () => {
  test('should be importable', () => {
    expect(ComprehensiveValidationWorkflow).toBeDefined();
  });
});
