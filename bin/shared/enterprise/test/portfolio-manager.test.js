import { PortfolioDeploymentManager } from '../portfolio-manager.js';

describe('PortfolioDeploymentManager', () => {
  test('should be importable', () => {
    expect(PortfolioDeploymentManager).toBeDefined();
  });
});
