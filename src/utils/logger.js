/**
 * Logger - Library Export
 * 
 * Re-exports Logger from bin/ for library use.
 * Gets compiled to dist/ for published package.
 */

export { Logger } from '../../lib/shared/logging/Logger.js';

